#include<bits/stdc++.h>

using namespace std;

/*
 * dp[i][m] = number of array upto index i that matches the array with m as a value when A[i] = 0
 * 

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	
	return 0;
}
 
