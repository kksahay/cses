#include<bits/stdc++.h>

/*
 * dp[i][j] = number of ways to reach i, j
 * dp[0][0] = 0
 * dp[i][j] = dp[i - 1][j] + dp[i][j - 1] iff (i, j) != '*'
*/

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	return 0;
}

