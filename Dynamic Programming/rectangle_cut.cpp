#include<bits/stdc++.h>

using namespace std;

/*
 * dp[i][j] = number of moves required for rectangle of height i and width j
 * dp[i][j] = i / j if i % j == 0
*/

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	int a, b;
	cin >> a >> b;
	
	return 0;
}
 
